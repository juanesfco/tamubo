# Experiments

Store reproducible studies and benchmark outputs here.

Layout:

- `experiments/<framework>/experiment*/`
- include a script or notebook that can regenerate the reported results
- keep logs, figures, and serialized artifacts inside each experiment folder

Framework folders in this repo:

- `experiments/exactbo/`
- `experiments/gpugp/`
- `experiments/mobbo/`
