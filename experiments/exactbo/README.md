# ExactBO Experiments

Experiments for `tamubo.exactbo` live under this directory.

Current contents:

- `experiment1/`: baseline serial ExactBO, good to compare results but noted time unfeasibility
- `experiment2/`: ExactBO benchmark vs gridBO and gradBO
