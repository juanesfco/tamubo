# Examples

Keep lightweight, runnable examples here.

Organization rule:

- one folder per framework (`examples/exactbo/`, `examples/gpugp/`, `examples/mobbo/`)
- each example should run from the repo root
- avoid large generated outputs in this folder
